import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Woo_ionic_angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
